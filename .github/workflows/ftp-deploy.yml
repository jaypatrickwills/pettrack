name: FTP Deploy

on:
  push:
    branches:
      - main

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Deploy to FTP server
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: server5.shared.spaceship.host
          username: admin@petstravelinternational.com
          password: &n50nda3(ADy
          # server-dir: /home/xflpekzi/domains/labradorpuppies.elementfx.com/public_html/
          # local-dir: ./   # Make sure this is relative to your repo root
          # dangerous-clean-slate: true
